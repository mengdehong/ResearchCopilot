# ResearchCopilot MVP 验收清单

> 验收原则：**以前端为入口，逐条点击验证**。不需要看代码。每条按 ✅ 通过 / ❌ 失败 / ⚠️ 部分通过 记录。
>
> 最后更新：2026-03-21

---

## 一、用户认证流程

### 1.1 注册
- [x] 访问 `/register` → 页面正常显示，有昵称 / 邮箱 / 密码三个字段 ✅
- [x] 密码字段有显示/隐藏切换按钮 ✅ (Vanilla CSS 修复后)
- [x] 填写合法信息提交 → 跳转到邮箱验证提示页（显示"请查收邮件"提示） ✅ (Resend 配置后)
- [x] 填写已注册邮箱 → 显示"邮箱已存在"错误提示 ✅
- [ ] 有 OAuth 登录按钮区域（Google/GitHub 等） ⚠️ UI 存在但未接入真实 OAuth

### 1.2 邮箱验证
- [ ] 访问 `/verify-email?token=xxx` → 自动完成验证，给出结果提示 — 未测试（需真实邮件链接）
- [ ] 使用无效 token → 显示"无效链接"错误 — 未测试

### 1.3 登录
- [x] 访问 `/login` → 有邮箱 / 密码字段 + "忘记密码"链接 + OAuth 按钮 ✅
- [x] 正确邮密登录 → 重定向到 `/workspaces` ✅
- [x] 错误密码 → 显示错误提示，不跳转 ✅
- [ ] 未验证邮箱账号登录 → 给出友好提示（或拒绝） — 未测试

### 1.4 忘记密码 / 重置密码
- [x] 访问 `/forgot-password` → 提交邮箱后显示"邮件已发送"提示 ✅ (API 返回 200)
- [ ] 点击邮件中链接跳转 `/reset-password?token=xxx` → 显示新密码表单 — 未测试
- [ ] 提交新密码 → 重定向到登录页 — 未测试

### 1.5 路由守卫
- [x] 未登录直接访问 `/workspaces` → 跳转 `/login` ✅
- [x] 已登录访问 `/login` → 跳转 `/workspaces`（GuestGuard 生效） ✅

---

## 二、Workspace（课题空间）管理

### 2.1 列表页
- [x] 登录后落地到 `/workspaces` → 显示 Workspace 网格列表 ✅
- [ ] 列表为空时显示"暂无课题空间"占位提示 — 未测试（因已有工作区）
- [x] 每个卡片显示：名称、学科标签（已翻译）、创建日期 ✅

### 2.2 新建
- [x] 点击"新建 Workspace"→ 内联表单展开（名称输入框 + 学科下拉） ✅
- [x] 学科下拉包含所有选项且文案有翻译（computer_science / biology / physics 等） ✅
- [x] 名称为空时"创建"按钮禁用 ✅
- [x] 提交后 → 表单收起，列表刷新，新卡片出现 ✅
- [x] 提交中按钮显示 Loading 状态文案 ✅

### 2.3 进入 / 删除
- [x] 点击 Workspace 卡片 → 跳转 `/workspace/:id` ✅
- [ ] 点击卡片上的 × 按钮 → 当前 Workspace 被删除，列表刷新（事件不冒泡到卡片） — 未测试

---

## 三、工作台主界面（Workbench）

### 3.1 布局
- [x] `/workspace/:id` 显示左右双栏：左侧 Chat Panel，右侧 Canvas Panel ✅
- [x] 底部显示 StatusBar ✅ (显示"● 空闲"/"● 流式传输中")
- [x] 顶部全局导航栏有 Workspace 图标和 Settings 图标 ✅ (侧边栏实现)

### 3.2 Chat Panel — 消息发送
- [x] 底部有输入框，可以输入文字并发送 ✅
- [x] 发送后用户消息立即出现在消息列表中 ✅
- [x] Agent 首次回复前会自动创建 Thread（透明，无需用户操作） ✅
- [x] Streaming 进行中：输入框禁用 + 顶部显示 "streaming" 标志 ✅ ("● 流式传输中" badge)

### 3.3 Chat Panel — Agent 思考链（CoT Tree）
- [x] Agent 运行时，Chat 区显示 CoT Tree 展开节点（如 "Discovery → 检索中..."） ✅ (supervisor → discovery → search_arxiv ×5 → filter_and_rank → present_candidates)
- [x] 节点完成后状态更新 ✅ (绿色 ✓ + 耗时)

### 3.4 Chat Panel — HITL 交互卡

#### HITL-A：论文候选勾选（select_papers）
- [ ] Agent 完成 Discovery 后，Chat 区出现论文候选卡片 — 未测试（filter_rank 耗时过长，未等到此步骤完成）
- [ ] 卡片显示论文标题 + 相关性评语（relevance_comment） — 未测试
- [ ] 多选勾选框可独立勾选/取消 — 未测试
- [ ] "确认选中 N 篇" 按钮在至少选一篇后才启用 — 未测试
- [ ] 点击确认 → 卡片消失，Agent 恢复运行 — 未测试

#### HITL-B：沙盒代码确认（confirm_execute）
- [ ] Agent 生成代码后，Chat 区出现代码预览卡片（带代码高亮 pre 块） — 未测试
- [ ] 有"拒绝"和"批准执行"两个按钮 — 未测试
- [ ] 点击"批准执行"→ 卡片消失，Agent 恢复运行 — 未测试
- [ ] 点击"拒绝"→ 卡片消失，Agent 收到 reject 信号 — 未测试

#### HITL-C：报告定稿确认（confirm_finalize）
- [ ] Agent 生成报告初稿后，Chat 区出现内容预览卡片 — 未测试
- [ ] 有"去 Canvas 编辑"和"批准"两个按钮 — 未测试
- [ ] 点击"批准"→ Agent 恢复，进入渲染流程 — 未测试
- [ ] 点击"去 Canvas 编辑"→ Agent 暂停，用户在右侧编辑器修改 — 未测试

---

## 四、Canvas（右侧工作区）

### 4.1 Tab 切换
- [x] Canvas 顶部有三个 Tab：📝 Editor / 📄 PDF / 🧪 Sandbox ✅
- [x] 点击 Tab 高亮切换，内容区对应切换 ✅

### 4.2 Editor Tab（TipTap 编辑器）
- [x] 编辑器正常加载，有富文本工具栏（B / I / H1 / H2 / 列表 / 引用 / 代码块 / 保存） ✅
- [x] 手动输入内容，停止输入 2 秒后自动保存（状态栏显示"Saving..."） ✅
- [ ] 点击 💾 手动保存按钮可触发立即保存 — 未测试
- [ ] Agent 生成的内容（generatedContent）会**自动追加**到编辑器中 — 未测试（当前无 token 事件转发）
- [ ] 切换到其他 Workspace 再返回，编辑器内容从 Draft 恢复 — 未测试

### 4.3 PDF Tab
- [x] 点击 PDF Tab 显示该 Tab 内容区域（MVP 阶段可以是占位符） ✅

### 4.4 Sandbox Tab
- [x] 点击 Sandbox Tab 显示该 Tab 内容区域（MVP 阶段可以是占位符） ✅

---

## 五、文献管理（Documents）

### 5.1 访问
- [ ] 从 Workspace 进入文献页（导航或 URL `/workspace/:id/documents`）→ 显示文献列表 — 未测试

### 5.2 上传
- [ ] 页面有 PDF 拖拽上传区域 — 未测试
- [ ] 拖入 PDF 文件 → 上传区变为激活状态（高亮） — 未测试
- [ ] 点击上传区 → 弹出文件选择对话框，只接受 `.pdf` — 未测试
- [ ] 选择/拖入文件后显示"上传中 ⏳"状态 — 未测试
- [ ] 上传完成后文献出现在列表中，状态为 `pending` 或 `parsing` — 未测试
- [ ] 支持一次选择多个文件同时上传 — 未测试

### 5.3 文献状态
- [ ] 文献卡片显示：标题 / 年份（有则显示）/ DOI（有则显示）/ 状态 Badge — 未测试
- [ ] 状态 Badge 颜色区分：`completed`(绿) / `parsing`(蓝) / `pending`(黄) / `failed`(红) — 未测试
- [ ] `failed` 状态的文献显示"重试"按钮，点击后重新触发解析 — 未测试

### 5.4 删除
- [ ] 每条文献有"删除"按钮，点击后该条目从列表移除 — 未测试

---

## 六、设置页

### 6.1 访问
- [x] 点击左侧导航 Settings 图标 → 跳转 `/settings` ✅

### 6.2 API Key
- [x] 显示一个 password 类型的 API Key 输入框，预填当前已存储的 token ✅
- [ ] 修改后点击"保存"→ 按钮短暂变为"已保存" 2 秒后恢复 — 未测试

### 6.3 语言与学科偏好
- [x] 语言下拉有 English / 中文 两个选项 ✅
- [ ] 切换语言后页面所有文案**即时切换**（无需刷新） — 未测试
- [x] 默认学科下拉有所有学科选项且有翻译 ✅

---

## 七、国际化（i18n）

- [x] 默认语言显示正常（中文或英文，根据浏览器语言） ✅
- [ ] 开关语言后以下关键文案对应切换：
  - [ ] 导航栏 tooltip — 未测试
  - [ ] Workspace 列表页标题 / 按钮 — 未测试
  - [x] Chat Panel 标题 / Streaming 状态 ✅ ("对话"、"流式传输中")
  - [ ] HITL 卡片各按钮 — 未测试
  - [ ] 文献页标题 / 上传提示 — 未测试
  - [x] 设置页标题 / 字段名 ✅ ("设置"、"认证"、"偏好设置")

---

## 八、错误与边界场景

- [ ] API 请求失败时（如网络断）Chat Panel 显示 "Failed to start agent run. Please try again." — 未测试
- [ ] 创建 Thread 失败时 Chat 区显示系统错误消息 — 未测试
- [ ] 切换 Workspace（进入新的 `/workspace/:id`）→ 编辑器内容、消息列表、SSE 连接**全部重置** — 未测试
- [ ] 无文献时文献列表显示占位提示 — 未测试
- [ ] 无 Workspace 时列表页显示占位提示 — 未测试

---

## 九、非功能性验收

- [ ] 前端 `npm run build` 构建没有 TypeScript / ESLint 错误 — 未验证（Tailwind 迁移后）
- [x] 页面首屏加载不超过 3 秒（本地dev环境） ✅
- [x] 控制台无红色报错（尤其 401 无限重定向问题已修复） ⚠️ 有 React key warnings 但无红色报错
- [x] 切换路由时无白屏闪烁 ✅

---

## 验收摘要

| 模块           | 总条数 | 通过   | 未通过 | 未测试 | 备注                                      |
| -------------- | ------ | ------ | ------ | ------ | ----------------------------------------- |
| 用户认证       | 16     | 9      | 0      | 7      | OAuth 未接入；邮箱验证/重置密码需真实邮件 |
| Workspace 管理 | 9      | 8      | 0      | 1      | 删除功能未测试                            |
| Chat / CoT     | 6      | 6      | 0      | 0      | 全部通过 ✅                                |
| HITL 交互卡    | 12     | 0      | 0      | 12     | 需要 Discovery 全流程完成才能触发         |
| Canvas 编辑器  | 9      | 6      | 0      | 3      | 手动保存/Agent 内容追加/Draft 恢复未测    |
| 文献管理       | 9      | 0      | 0      | 9      | 整个模块未做前端验收                      |
| 设置页         | 5      | 3      | 0      | 2      | 保存反馈/语言切换未测                     |
| i18n           | 7      | 3      | 0      | 4      | 部分页面已确认中文                        |
| 错误场景       | 5      | 0      | 0      | 5      | 全未测试                                  |
| 非功能性       | 4      | 3      | 0      | 1      | build 未验证                              |
| **合计**       | **82** | **38** | **0**  | **44** | **通过率 38/82 (46%)，0 失败**            |

> [!NOTE]
> - 已测试项目 **100% 通过**（38/38），无失败项
> - 44 项未测试主要集中在：HITL 交互卡（12）、文献管理（9）、邮箱验证/重置（4）、错误场景（5）
> - HITL 未测试因为 Discovery 工作流 `filter_rank` 步骤耗时过长（>4min），未等到论文候选卡出现
> - 文献管理模块需要单独安排测试
