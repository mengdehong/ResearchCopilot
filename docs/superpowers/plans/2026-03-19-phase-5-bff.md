# Phase 5: FastAPI BFF Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** 实现 FastAPI BFF 层：认证中间件、Repository 层、API Router、SSE 事件流、文件代理。

**Architecture:** 分层架构 `Router → Service/Repository → DB`。FastAPI 的 `Depends()` 注入 Session 和服务实例。

**前置条件：** Phase 1（ORM models）+ Phase 2（LLM Gateway）

**对应设计文档：**
- [FastAPI BFF 设计](file:///home/wenmou/Projects/ResearchCopilot/docs/superpowers/specs/2026-03-19-fastapi-bff-design.md) — 全文

---

## 文件结构

```
backend/
├── api/
│   ├── __init__.py
│   ├── deps.py                # [NEW] 依赖注入（get_session, get_current_user）
│   ├── middleware.py          # [NEW] Auth 中间件 + trace_id 注入
│   └── routers/
│       ├── __init__.py
│       ├── workspaces.py      # [NEW] CRUD /workspaces
│       ├── threads.py         # [NEW] CRUD /threads + SSE 事件流
│       ├── documents.py       # [NEW] 上传 + 解析状态 /documents
│       └── health.py          # [MOVE] /health 迁移到 router
├── repositories/
│   ├── __init__.py
│   ├── workspace_repo.py      # [NEW] WorkspaceRepository
│   ├── thread_repo.py         # [NEW] ThreadRepository
│   └── document_repo.py       # [NEW] DocumentRepository
```

---

## Task 1: 依赖注入 — deps.py

- [ ] **Step 1: 实现 deps.py** — `get_session()` 从 `app.state.session_factory` 获取 AsyncSession；`get_current_user()` 解析 JWT
- [ ] **Step 2: 编写测试**
- [ ] **Step 3: Commit**

---

## Task 2: Auth 中间件 — middleware.py

- [ ] **Step 1: 实现 middleware.py** — trace_id 注入（structlog contextvars）+ JWT 校验
- [ ] **Step 2: 编写测试**
- [ ] **Step 3: Commit**

---

## Task 3: Repository 层

- [ ] **Step 1: 实现 workspace_repo.py** — async CRUD（create, get_by_id, list_by_owner, soft_delete）
- [ ] **Step 2: 实现 thread_repo.py** — async CRUD + status 更新
- [ ] **Step 3: 实现 document_repo.py** — async CRUD + parse_status 更新
- [ ] **Step 4: 编写测试** — `tests/integration/test_repositories.py`（需要 PostgreSQL）
- [ ] **Step 5: Commit**

---

## Task 4: API Routers

- [ ] **Step 1: 实现 workspaces.py** — `POST /workspaces`, `GET /workspaces`, `GET /workspaces/{id}`, `DELETE /workspaces/{id}`
- [ ] **Step 2: 实现 threads.py** — `POST /threads`, `GET /threads`, `GET /threads/{id}/events`（SSE）
- [ ] **Step 3: 实现 documents.py** — `POST /documents/upload`, `GET /documents/{id}`, `GET /documents/{id}/status`
- [ ] **Step 4: 迁移 health.py** — 从 main.py 移至 router
- [ ] **Step 5: 更新 main.py** — 注册所有 Router + 中间件
- [ ] **Step 6: 编写 API 测试** — `tests/unit/test_api_*.py`（httpx AsyncClient）
- [ ] **Step 7: Commit**

---

## Task 5: SSE 事件流

- [ ] **Step 1: 实现 SSE 适配器** — 将 LangGraph streaming 事件翻译为 SSE 格式
- [ ] **Step 2: 编写测试**
- [ ] **Step 3: Commit**

---

## 验证清单

| 检查项      | 命令                                                      | 期望结果         |
| ----------- | --------------------------------------------------------- | ---------------- |
| 依赖注入    | `uv run pytest tests/unit/test_deps.py -v`                | passed           |
| Repository  | `uv run pytest tests/integration/test_repositories.py -v` | passed           |
| API Routers | `uv run pytest tests/unit/test_api_*.py -v`               | passed           |
| 全量 lint   | `uv run ruff check backend/api/ backend/repositories/`    | 0 errors         |
| Smoke test  | `curl localhost:8000/api/v1/workspaces`                   | 401 Unauthorized |

---

**Phase 5 完成标志：** 所有 API 端点可调用 + 认证中间件拦截未登录请求 + Repository CRUD 通过集成测试 → 可进入 Phase 6。
